import React from 'react';

NotFound.propTypes = {
    
};

function NotFound(props) {
    return (
        <div>
            Oop!...Component is not found
        </div>
    );
}

export default NotFound;